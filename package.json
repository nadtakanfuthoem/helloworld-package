{
  "name": "helloworld-package",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "ci": "npm run build && npm run check-format && npm run check-exports && npm run test",
    "format": "prettier --write .",
    "check-format": "prettier --check .",
    "check-exports": "attw --pack . --ignore-rules=cjs-resolves-to-esm",
    "test": "vitest run",
    "dev": "vitest"
  },
  "author": "Nadtakan Futhoem",
  "description": "",
  "type": "module",
  "files": [
    "dist"
  ],
  "homepage": "https://github.com/nadtakanfuthoem/helloworld-package",
  "license": "MIT",
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.1",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  },
  "main": "dist/index.js"
}
